.row
  .span6.center
    .row-fluid
      %ul.thumbnails
        - @actions.each do |action|
          %li.span6
            %a.thumbnail.actionbutton{alt: action[:label], title: action[:label], href: action[:target]}
              .row-fluid
                .span4
                  -#= image_tag action[:img], alt: action[:label]
                  %i.icon-4x{class: action[:icon], style: "margin-top:5px;", alt: action[:label]}
                .span8
                  %p= action[:label]
